import React, { Component } from 'react';

export const LightBox = (props) => {
    return (
        <div>
            <LightBoxHeader title={props.title} />
            <LightBoxDisplay link={} mediatype={} />
        </div>
    )
}


const LightBoxHeader = ({title}) => {
    
}

const LightBoxDisplay = ({link, mediatype}) => {

}

